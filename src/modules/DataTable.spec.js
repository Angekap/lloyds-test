/*eslint-env mocha */

//import React from 'react'
import expect from 'expect'
//import { shallow } from 'enzyme'
//import DataTable from './DataTable'

describe('Savings page table', () => {
	// let actions = {}
	// it('Should initialise with JSON data', () => {
	// 	const el = shallow(<DataTable />)
	// 	expect(el.state('dataList')).toExist().toInclude({'Product': 'Fixed Saver','Interest rate': '2.2%','Minimum deposit': '500','Interest type': 'Fixed'})
	// }).skip() // Sorry, no time!
	// it('Should render a data table', () => {
	// 	const el = shallow(<DataTable actions={actions} />)
	// 	expect(el.contains("<table"))
	// }).skip() // Sorry, no time!
	it('Should test something meaningful', () => {
		expect(true).toBe(true)
	})
})
